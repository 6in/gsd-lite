# PLAN — <milestone slug>

- 作成: <日付> / gsd-lite-plan
- 入力: REQUIREMENTS.md / DECISIONS.md / RESEARCH.md

## 検証コマンド

impl の各ターンがテストに使うコマンド（プロジェクトに合わせて plan が確定する）:

```bash
<例: npm test / cargo test / pytest>
```

## Tasks

<!--
タスク粒度の品質基準（2026-09-16 改定 — codelist-core で 29 タスク = 29 ターンかかった反省）:

- 上限: 各タスクは「1 ターン（新規コンテキスト 1 回）で実装 + テスト + コミットまで完結する」
  大きさ。1 ターンは毎回 REQUIREMENTS / PLAN / PROGRESS を読み直す固定コストを払うので、
  小さすぎるタスクはターン数とトークンを無駄にする
- 統合の目安: **1 ターンで完結する範囲で、同じ種類の作業を 1 タスクにまとめる**。例:
  - ストアドプロシジャ / 関数: 同系統（管理系・参照系など）を 3〜4 本で 1 タスク
  - 文書: 同じ doctype の仕様書群（UC 群・SP 群・テスト仕様群）はそれぞれ 1 タスク
  - テスト: 対象 SP と同じタスクに含める（別タスクにしない）
  - 環境・型紙の vendoring: 1 タスク
- タスク数は **マイルストーン全体で 8〜15 タスクを目安**とし、必須の下限・上限にはしない。
  数より「1 ターンで実装 + テスト + コミットまで完結する」ことを優先する。
  小規模な要件を 8 タスクに増やす必要はない。15 を超える場合は、分割を維持する理由を
  PLAN.md の「メモ」に記録する。20 を超えたら統合できる箇所を再点検するが、無理に統合しない
- 手順は **2 段階**: ①まず細かく分割して洗い出す（受け入れ基準からの漏れを防ぐ。この段階では
  数を気にしない）→ ②1 ターンで完結する範囲で同種・同ファイル群のタスクを統合する。
  統合すると 1 ターンを超えるものや、「完了基準が独立していて、失敗時の切り戻し単位を
  分けたい」ものは分けたまま残す
- verify の差し戻しタスクは F1, F2... として末尾に追記される
- 並列サブ作業: タスクを「対象ファイルが重ならない独立した作業単位」に分けられるなら列挙する。
  impl は state の `subagents` が `auto` で実行エンジンがサブエージェントを使える場合、
  サブ作業ごとにサブエージェントを起動して並行実装し、親がまとめてテスト・コミットする。
  分けられない・互いに依存する・同じファイルを触るものは `なし`。3〜4 個までが目安。
  並列にできるタスクは 1 ターンで完結できる範囲が広がるので、②の統合でも考慮する
-->

- [ ] T1: <タスク名>
  - 完了基準: <テストで機械検証可能な形で>
  - 対象: <ファイルパス>
  - 依存: なし
  - 並列サブ作業: なし
    <!-- 例:
    - A: <内容>（対象: src/model.ts, tests/model.test.ts）
    - B: <内容>（対象: src/api.ts, tests/api.test.ts）
    -->

## メモ

<plan が impl に申し送りたい設計上の注意点>
