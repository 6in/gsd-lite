---
description: gsd-lite の振り返り（PMI）を手動で追加する。記録だけを根拠に .gsd-lite/reflect/ に書く
---
Use the `skill` tool to load the skill named `gsd-lite-reflect` (project skill in
`.opencode/skills/gsd-lite-reflect/SKILL.md`; if it is missing, tell the user to update
project skills with `/gsd-lite-init` first) and follow it exactly in manual mode: write an
additional reflection covering the commits since the previous one, commit only the
reflection file and PROGRESS.md, and leave state.json's phase / next_command / turn unchanged.
Notes from the user: $ARGUMENTS
