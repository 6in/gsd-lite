---
description: gsd-lite の足場（.gsd-lite/・プロジェクト用スキル）をこのプロジェクトに生成する
---
Use the `skill` tool to load the skill named `gsd-lite-init`, then follow its instructions
exactly for the OpenCode host (templates in `~/.config/opencode/gsd-lite/templates/`,
project skills in `.opencode/skills/`). Ask the user in plain conversation where the
skill says to ask. Arguments from the user: $ARGUMENTS
