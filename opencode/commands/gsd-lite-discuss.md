---
description: gsd-lite の要件確定（discuss）を対話で行い、ループ起動の準備をする
---
Use the `skill` tool to load the skill named `gsd-lite-discuss` (project skill in
`.opencode/skills/gsd-lite-discuss/SKILL.md`; if it is missing, tell the user to run
`/gsd-lite-init` first) and follow it exactly. Ask the user in plain conversation where
the skill says to ask. What the user wants to build: $ARGUMENTS
